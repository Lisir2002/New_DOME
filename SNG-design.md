# SNG v0.3：三身份模型系统 —— 控制器 / 插件 / Echo-er 设计文档

> 版本：v0.3.2（机制设计阶段 · **通用架构** · 黑板池式 + 电源管理）
> 定位：**通用架构**——以"唯一主控制器 + 可下载插件 + 独立 Echo-er"的三身份模型系统为骨架，不绑定任何领域；领域适配 = **下载对应插件文件 → 注册 → 即用**，无需重训核心。首个验证基准：文本游戏。
> 内存预算：主控制器 30~50MB，每个插件 ≤80MB，Echo-er ≤80MB；插件默认关机、仲裁放行才开机；稠密全激活、中英双语。

---

## 1. 目标与硬约束

| 约束 | 要求 |
|---|---|
| 架构 | **通用三身份骨架**：唯一主控制器（只调配）+ 可下载插件（能力各异）+ Echo-er（负责整个分析生成输出） |
| 内存 | 主控制器 **30~50MB**；每个插件文件 **≤80MB**；Echo-er 文件 **≤80MB** |
| 激活 | 每个已加载模块**内部全激活**（稠密，无稀疏路由）；模块间按需动态调度（这是设计意图，不是 MoE） |
| 调度 | 控制器**根据任务动态调用**插件 |
| 通用性 | **不绑定任何领域**；骨架与领域无关 |
| 领域适配 | **下载对应插件文件 → 注册 → 即用**，无需重训核心（核心卖点） |
| 新颖度 | 市面没有的独特架构，能写成论文 |
| 基准 | 文本游戏作为**首个验证基准**（状态可验证、数据可自动生成，最利于证明机制） |
| 数据流程 | 插件结果**先验证再生成** |
| 电源管理 | 插件默认**关机**（零内存/零算力/零延迟）；被仲裁放行才开机 |
| 消息总线 | 主模型 →【指令池】→ 插件 →【结论池】→ Echo-er（黑板架构） |

**为什么选文本游戏做首个验证基准**（不是定位，是证明机制最有利的场景）：
- 计算本质是显式、可验证的世界状态 + NL 动作理解 + 生成，能最直接检验"状态一致性 / 调度正确性 / 先验证再生成"。
- 状态迁移可观测、可自动生成（TextWorld 式引擎）→ 从零训练数据可解。
- 领域窄 → 各身份模型小、目标明确 → 从零训练现实可行。
- 机制验证通过后，其余领域**只换插件（下载即用），架构不变**。

---

## 2. 三身份架构总览

核心命题：**把"决策 / 知道 / 说话"解耦为三个独立、可替换、可组合的模型身份。**

```
用户输入
   │
   ▼
① 主控制器 (30~50MB) ──────── 常驻（通用骨架），理解 + 状态引擎判任务，只调配
   │  写【指令池】
   ▼
【指令池】────────────── 集中仲裁 → 插件开机/关机名单
   │  插件自判断器（纯规则）双重校验，防主模型分配错误
   ▼
② 插件 (各 ≤80MB) ──────── 通用能力插件 + 领域插件（可下载）
   │  默认关机；被放行的才开机处理
   │  结果有条理写【结论池】
   ▼
【结论池】── 验证器（内置）校验 ──▶ 通知 Echo-er
   │
   ▼
③ Echo-er (≤80MB) ────── 读结论池，处理优化总结，输出
   │
   ▼
最终输出
```

- **谁决策** → 主控制器
- **谁知道** → 附属插件
- **谁说话** → Echo-er

### 为什么三者分离是研究级亮点
1. **小控制器是稀缺品**：编排通常是大模型专利，30~50MB 专训调度 LLM 是研究空白。
2. **插件可独立替换/升级**：加能力 = 加文件，能力阶梯天然成立；**领域适配 = 下载插件文件即注册即用，核心骨架不动**。
3. **Echo-er 分离生成**：生成质量（双语、叙事风格）可独立强化，不被控制器/插件训练牵制。
4. **有状态调度**：控制器基于内置状态引擎调配，调度决策可验证，优于 LLM agent 的无根基工具调用。
5. **组合单调性（N+1≥N）**：加插件应让系统更强，是贯穿的核心命题。

---

## 3. 模块设计

### 3.1 主控制器（唯一主模型，30~50MB）—— 新架构的心脏

**职责**：读入输入 → 结合状态 → 输出调度决策 + 状态更新。是一个**带持久可验证状态的计算模型**（不是"LLM 随手选插件"）。

**内部组成（统一前向计算图）**：
- **编码器**：小型双语 Transformer，把输入 token 编码为上下文 h_t。
- **状态读写层 WS**（§3.2）：持久状态矩阵 S ∈ R^{128×128}，跨回合读写。
- **可编译规则记忆层 CRM**（§3.2）：规则表，训练时可微、推理时编译为 DFA。
- **自验证循环头 SVSL**（§3.3）：计算图内的验证循环。
- **调度头**：从 h_t + 状态读取生成调度决策 d_t（task_types[] / candidate_plugins / complexity）。

**主控制器前向（每回合）**：
```
x_t ──► Encoder ──► h_t
        ├──► WS Read ──► r ──► 与 h_t 融合 ─► h'_t ─► Dispatch head ─► d_t
        ├──► CRM query(h'_t + 状态模式) ──► 规则约束 / 迁移候选
        ├──► WS Write：生成状态更新候选 Δ
        └──► SVSL 循环（≤K 次）：
                s' = apply(S, Δ)
                v  = Verify(s', CRM 规则, 一致性检查)
                通过 → S := s'；失败 → Δ = Correct(Δ, 失败原因)
                仍失败 → 保留 S + 告警
输出：d_t + S_t + 验证结果
```
- 调度决策与状态管理是**同一前向的一部分** → 端到端可训练。
- 不直接产出最终叙述——那是 Echo-er 的职责。

### 3.2 状态引擎（WS 状态读写层 + CRM 规则记忆层，内置）

**WS 状态读写层**：
- **结构**：持久状态矩阵 `S ∈ R^{128×128}`。每个槽 = **槽键**（可寻址表示）+ **槽值**（内容向量）。
  - 类型化槽：领域 schema 绑定固定槽位（如 `flag.door_locked → 槽#5`），语义固定。
  - 自由槽：无固定语义，content-addressable，模型自定存什么。
- **读取（content-based）**：`α = softmax(q·K/√d)`，`r = Σ αᵢVᵢ`；多查询头并行做**关系读取**（"位置在 X 且背包有 Y"）。
- **写入（混合寻址 slot addressing）**：
  - 类型化槽：**语义固定直写**（schema 直接映射槽位，无需学寻址）→ 可精确验证。
  - 自由槽：**content-based 软寻址**（写查询 q_w 打分所有槽，软写或 Gumbel 稀疏写）。
- **更新操作（受控枚举，验证器可精确检查）**：`set / add / append / remove / clear`，由动作类型决定。
- **持久性**：S 跨回合保留——区别于 KV cache（KV 是输入缓存，S 是有语义可读写的模型状态）。
- **闭环**：读取 r 影响决策（读），决策生成 Δ 更新状态（写）。

**CRM 可编译规则记忆层**：
- **结构**：规则表 `{ (状态模式, 动作模式, 迁移) }`，每条 = 三个可微嵌入 `E_pattern / E_action / E_transition`。
- **可微索引学习（训练时）**：
  - 查询 `q = f(状态特征, 动作特征)`，`α = softmax(q·E_rule/√d)` 在规则上软注意力。
  - 稀疏正则逼向单一匹配；匹配损失 `L_match = CE(α, 引擎标注真值规则)`。
  - 学成后 softmax 收敛到确定性 argmax。
- **编译为 DFA（推理/加载时）**：
  - 软匹配硬编码（argmax），构造迁移表 `(状态模式, 动作) → 迁移`。
  - 推理时查表 O(1) 确定性迁移 → **一致性是结构性保证**。
- **编译可靠性**：
  - 编译门槛：匹配置信不足的规则**不编译**（留软记忆），避免错误固化。
  - 冲突检测：同一 (状态, 动作) 出不同迁移 → 报冲突，按 schema 优先级消解。
  - 覆盖补全：DFA 未覆盖的 (状态, 动作) → 视为不合法动作，交验证器拒绝。
- **与 SVSL 衔接**：训练时 SVSL 用软 CRM 打分；推理时用编译 DFA 做确定性检查（s' 是否可达）。

### 3.3 验证器（SVSL 自验证循环头，内置）
- **循环（≤K 次）**：候选更新 s' → 验证打分 v = Verify(s', CRM 规则, 一致性/冲突检查) → 通过则接受；失败则用修正头产出修正 Δ 重试；K 次仍失败 → 保留旧状态 + 告警。
- **验证打分器**：小判别头（h_t + s' + 规则约束 → 通过概率），或直接用 CRM DFA 的确定性检查。
- **修正头**：小回归/生成头，(h_t, s', 失败原因) → 修正后的更新。
- **为什么在计算图内**：验证是训练时的计算结构（损失含"状态更新必须通过验证"），模型学会主动提出一致更新；不是部署后的安全过滤器。
- 对插件返回的分析/状态更新同样**先验证再放行**给 Echo-er（先验证再生成）。

### 3.4 插件体系（各 ≤80MB，可下载）

插件分两类：
- **通用能力插件**：与领域无关，随骨架发布（记忆、文字理解、任务划分、双语转换等）。
- **领域插件**：按需从插件市场下载（文本游戏领域知识包、法律咨询知识包、医疗问答知识包等）。

每个插件 = 一个独立模型文件 + 一个专项能力，通过统一接口接入：

| 插件 | 类型 | 能力 |
|---|---|---|
| 记忆（SGNM） | 通用 | 长叙事回忆、事件切分、惊讶门控压缩 |
| 文字理解 | 通用 | 深层 NL 语义、动作解析/接地 |
| 任务划分 | 通用 | 把复杂目标拆成子任务序列 |
| 双语转换（CLSA） | 通用 | 中英互译/转写，语言切换不丢状态 |
| 领域知识包 P1..N | 领域 | 各领域一包，可下载（如：文本游戏包、法律包、医疗包、客服包、代码分析包……） |

每个插件附带一个**纯规则匹配的自判断器（Judge）**，用于**预防主模型分配错误**（双层校验）：
- 仲裁器点名该插件 → 判断器确认，防误唤醒；
- 仲裁器漏派 → 判断器发现强相关 → 主动请求开机，防漏派。

### 3.5 Echo-er（≤80MB，独立文件）
- **职责**：专门负责整个分析生成输出——接收"验证后的可信分析 + 状态 + 场景"，生成最终叙述/回复。
- 生成质量（双语、叙事风格、角色语气）可**独立强化**，不牵制控制器/插件。
- 可单独替换/升级，不影响调度链路。

### 3.6 插件接口协议（模型间互操作契约）

**原则**：不传自由文本、全结构化；消息头极轻（插件要快速分辨）；task_id 贯穿全流程；带可校验字段。

**序列化**：开发用 JSON（可读、易调试、纯规则可直接匹配）；交付用 CBOR/MessagePack（体积小、解析快）。同一 schema 双序列化器。

**Schema 治理**：统一 schema 注册表；`protocol_version` 版本兼容（只增不删、可选字段）。

**① DispatchRequest（主模型 → 指令池）**
```
{protocol_version, msg_type:"dispatch_request", task_id, timestamp,
 task_types:[...], complexity,       // 有序列表，首位主意图；仲裁阈值用
 payload:{user_text, parsed_action:{verb,target,...}|null, context},
 state_snapshot:{location, inventory[], flags{}, npc_states{}},  // 状态引擎
 goal, candidate_plugins[],          // 集中仲裁预筛
 priority, deadline_ms}
```

**② WakeNotice / WakeRequest（仲裁器→插件 / 插件自请求）**
```
{msg_type:"wake_notice"|"wake_request", task_id, plugin_id,
 reason:"arbiter_nomination"|"plugin_judge_self_request",  // 双层校验标记
 budget:{memory_mb, latency_ms}}     // 预算分配
```

**③ AnalysisResult（插件 → 结论池）**
```
{msg_type:"analysis_result", task_id, plugin_id, result_type,
 result_object,                      // 类型相关结构化结果
 confidence, claims[],               // 供验证器 SVSL 校验的原子断言
 cost:{memory_mb, latency_ms}, warnings[]}
```

**④ VerificationReport（验证器）**
```
{msg_type:"verification_report", task_id, plugin_id,
 verdict:"pass"|"fail"|"correctable", issues[], corrected, passed_to_echo}
```

**⑤ GenerationRequest（→ Echo-er）**
```
{msg_type:"generation_request", task_id, scene, state, facts[],
 style, language, max_tokens}        // max_tokens 是延迟控制杠杆
```

**⑥ ProbePing（插件探针心跳）**
```
{msg_type:"probe_ping", plugin_id, status:"off"|"probe_only", judge_rules_version}
```

**池式传递**：
- **指令池**：主模型写 DispatchRequest；仲裁器读 + 插件探针读（纯规则匹配）。
- **结论池**：开机插件写 AnalysisResult；验证器校验后生成 GenerationRequest 通知 Echo-er。

**端到端示例（"打开那扇锁着的门"）**：
1. 主模型 → 指令池：`DispatchRequest{task_types:["action.obj"], parsed_action:{verb:"open",target:"door"}, state_snapshot:{flags:{door_locked:true}}}`
2. 仲裁器 → `WakeNotice{state_engine, text_understanding, domain_kb}`
3. text_understanding 探针：task_types 含 action.obj 命中能力映射 → 确认开机
4. 插件 → 结论池：`AnalysisResult{result_type:"parse", claims:[{type:"state_change", slot:"flags", op:"set", value:"door_open", if:{has_key:true}}]}`
5. 验证器：`VerificationReport{verdict:"pass"}`
6. 主模型 → Echo-er：`GenerationRequest{scene, state, facts, language:"zh", max_tokens:60}`
7. Echo-er 输出："你拧动门把，门锁应声而开。"

> 关键设计点：**claims[]（原子断言）是验证器 SVSL 的输入**——插件给出带前置条件的可校验断言（如 `if:{has_key:true}`），验证器据此判 pass/fail，"先验证再生成"在协议层落地。

### 3.7 插件注册表与市场规范（下载即用的工程基础）

**插件清单（Manifest）**——每个插件文件自带 manifest.json：
```json
{
  "id": "sng.game.textworld",
  "name": "文本游戏领域知识包",
  "version": "1.0.0",
  "category": "domain",                 // universal(通用) | domain(领域)
  "domain_tags": ["textgame"],          // 领域标签，市场检索用
  "entry": {"model": "domain_textworld.lbnt", "runtime": "lbn_runtime>=1.2"},
  "format": "lbnt_ternary",             // LBNT 三值 / INT4
  "cost_card": {                        // 四维成本卡（出厂值）
    "memory_mb": 24,
    "latency_ms": {"p50": 120, "p95": 200},
    "accuracy": 0.91,                   // 决定能否跳过验证
    "gain_matrix": {
      "reasoning.puzzle": 0.35, "action": 0.20, "retrieval.kb": 0.15
    }
  },
  "capabilities": ["world_rules", "puzzle_solving"],  // 供仲裁规则引用
  "judge": {"rules_version": "1", "confirm": [...], "veto": [...]},
  "task_types": {                       // 该插件扩展的词表子类型
    "action": ["loco","obj","combat","dialog","inventory","nav"],
    "reason": ["puzzle"]
  },
  "dependencies": [{"id": "sng.text_understanding", "min_version": "1.0.0"}],
  "languages": ["zh", "en"],
  "signature": "sha256:..."             // 完整性/来源校验
}
```

**注册流程（下载即用的生命周期）**：
```
下载 → 签名/依赖校验 → 安装(入插件目录) → 注册(manifest 入表)
     → 成本卡入表 → judge 规则编译 → 词表扩展(task_types 并入) → 即用
```
- 依赖解析：缺运行时/缺依赖插件 → 提示一并下载或拒绝安装。
- 版本兼容：semver；`protocol_version` 必须兼容；单插件可热更新/回滚，骨架不动。

**注册表结构（PluginRegistry）**：
```
PluginRegistry:
  protocol_version
  plugins: [Manifest...]               // 已注册插件
  runtime: {lbn_version, mem_budget, max_concurrent_wake}
  task_taxonomy: {base: [...], extensions: {...}}   // 词表 = 核心 + 领域扩展
  rules: {...}                          // 编译后的 judge 规则
  gains_overlay: {plugin: {task_type: gain}}        // 运行时自学习覆盖出厂值
```

**市场索引（Marketplace Index）**：
- 远程索引：插件摘要 + 大小 + 版本 + 下载地址 + 签名。
- 离线安装：manifest + 模型文件直接放入插件目录即注册。
- 信任与安全：签名校验、权限声明（读写指令池/结论池的范围）。

**对接**：cost_card.gain_matrix → 背包分配 + 仲裁阈值 θ；accuracy → 验证器"高精度可跳过"；judge 规则 → §5.6 规则引擎；task_types → §5.6 词表领域扩展；gains_overlay → §5.4 运行时自学习回填。

---

## 4. 内存预算

| 组件 | 文件 | 预算 |
|---|---|---|
| 主控制器（调度 + 状态引擎 + 验证器） | 1 个 | **30~50MB** |
| 附属插件 | 每能力 1 个 | **各 ≤80MB**（通常仅几 MB~几十 MB） |
| Echo-er | 1 个 | **≤80MB** |

- 精度路线沿用 **LBNT（低比特原生训练，1.58bit 三值 / INT4）**，让每个文件都薄。
- 一个完整部署 = 主控制器 + 选定插件 + Echo-er；插件按任务动态加载，能力阶梯 = 可组合的插件数量。

---

## 5. 性能调度设计

### 5.1 三层调度总览

- **第 1 层 调度决策（Dispatch Decision）**：选什么——控制器判定任务、选插件子集+顺序+参数。
- **第 2 层 执行编排（Orchestration）**：怎么执行——执行图 DAG、并行、验证插入、降级兜底。
- **第 3 层 资源调度（Resource Scheduling）**：花多少资源——内存/延迟预算、能力阶梯自适应、带约束选择。

先决条件：**插件注册表**，每插件一张**四维成本卡**。

### 5.2 第 1 层 调度决策

- 输入：用户输入 + 状态快照 + 当前目标；输出：调度计划（有序插件子集 + 参数 + 优先级）。
- 决策形态：生成式 / 分类式 / 混合（先分类任务类型，再小步生成细节，推荐）。
- 训练：引擎 rollouts 自动标注"最优调度"→ SFT，稳定后上 RL（调度→目标达成→reward）。
- 评测：**调度正确率**（选中插件是否最优，引擎给 ground truth）。

### 5.3 第 2 层 执行编排

- 执行图（DAG）：可并行插件并发，有依赖插件串行。
- 验证插入：默认每插件结果**先验证再放行**；高精度插件可跳过校验。
- 降级：插件超时/低置信 → 跳过 + 控制器兜底，不阻塞回合。
- 运行时：轻量编排引擎（调度代码，非模型）。

### 5.4 第 3 层 资源调度（核心）

**电源管理（插件三态生命周期）**：
- **关机 OFF**（默认）：零内存 / 零算力 / 零延迟，**不进入任何预算式子**。
- **唤醒 WAKE**：被仲裁放行 → 加载处理。
- **探针 PROBE**：纯规则匹配的监听，成本≈0。

**开机决策（双层校验）**：
```
开机 = 集中仲裁(主推) + 插件自判断器(纠错)
  · 仲裁器点名 → 插件判断器确认，防误唤醒
  · 仲裁器漏派 → 插件判断器强相关，主动请求开机，防漏派
```

**四维成本卡**：`CARD(p) = (g[p][τ], mem[p], lat[p], acc[p])`
- g 能力增益：任务类型相关 → 增益矩阵 `[插件][任务类型]`，非标量。
- mem 内存成本、lat 延迟成本、acc 精度（决定能否跳过验证）。

**内存预算模型**：
```
MEM_peak = MEM_常驻(主模型 + 仲裁/探针 + Echo-er) + Σ_{开机 p} mem[p]
约束: MEM_peak ≤ MEM_BUDGET
```
- **关机插件不进入式子**（零占用，比 LRU 缓存更彻底）。
- 工程含义：**"同一时刻开机几个插件"是硬指标**，由仲裁与预算共同决定。

**延迟预算模型**：
```
LAT_turn = t_ctl(决策) + Σ并行波(插件) + t_ver(验证) + L × t_per_token(Echo-er)
```
- 控制杠杆：输出长度上限 L、插件并行波、高精度插件跳过验证。

**能力阶梯自适应（仲裁阈值自适应）**（"组合数量决定强弱"的运行时实现）：
| 档位 | 开机 | 场景 |
|---|---|---|
| L0 简单 | 主模型 + Echo-er | 看地图、打招呼 |
| L1 中等 | + 状态引擎 + 文字理解 | 开箱子、拿物品 |
| L2 复杂 | + 记忆 + 任务划分 + 领域知识 | 解谜、多目标 |
- 任务越复杂，仲裁阈值越低（放行更多插件开机）；简单任务阈值高（少开机）。
- 阶梯是**候选集**而非死档，最终由仲裁 + 预算分配收敛。

**预算分配算法（带约束选择）**：
```
目标: max Σ_{p∈S} g[p][τ]
约束: Σ_{p∈S} mem[p] ≤ MEM_BUDGET
      Σ并行波 lat ≤ LAT_BUDGET
```
- 插件数小（~5~10）→ **0/1 背包精确 DP 可解**，无需近似。
- 备选：按增益密度 `g/mem` 贪心取前 K。
- 增益带不确定性：期望增益（开发）/ 上置信界（探索），随部署数据收敛。

**运行时自学习**：每回合回填真实结果（任务成败、实际内存/延迟、插件使用）→ 在线更新 g 估计 → 成本卡自适应部署负载，调度器自优化。

**资源调度评测**：
- 资源利用率（难任务接近预算、易任务低开销）
- 性价比：每单位内存/延迟的任务成功率增益
- 阶梯单调性：L1 ≥ L0、L2 ≥ L1（组合单调性在阶梯上的具体化）
- Pareto 权衡曲线：任务成功率 vs 延迟，自适应调度器应贴合前沿

### 5.5 运行时数据流程（池式）

1. 输入进入主控制器（常驻）。
2. 控制器结合状态引擎判定任务类型 + 复杂度，写【指令池】。
3. 集中仲裁放行候选插件；插件自判断器（纯规则）双重校验，防分配错误。
4. 被放行插件开机处理，结果有条理写【结论池】。
5. 验证器校验（高精度插件可跳过），通知 Echo-er。
6. Echo-er 读结论池，处理优化总结，输出（中英文皆可）。

### 5.6 仲裁与判断规则库

**task_type 通用分类规范 v2**（DispatchRequest 的受控词表；随领域插件自动扩展）：
```
task_type（顶层按"认知操作"划分，互斥；细类标注 判定信号 / 所需能力）
├── comprehension 理解
│   ├── parse（意图/实体） ├── classify（分类） ├── extract（抽取）
│   ├── summarize（总结） └── qa（问答）
├── reasoning 推理
│   ├── plan（规划/分解） ├── logic（逻辑/数学） └── decision（决策）
├── code 代码（独立横切能力）
│   ├── gen ├── explain ├── debug └── exec
├── generation 生成
│   ├── text（创作/改写） ├── dialogue（对话） └── format（结构化输出）
├── retrieval 检索/记忆
│   ├── memory（记忆召回） ├── doc（文档检索） └── kb（领域知识）
├── lingua 语言/转换
│   ├── translate ├── define └── switch
└── system 系统
    ├── help ├── meta └── unknown（兜底）
```

**判定信号 + 能力映射**（每个细类配两列，喂给纯规则匹配/分类训练/仲裁查表）：
| 细类 | 判定信号 | 所需能力 |
|---|---|---|
| parse | 意图动词 + 实体 | 文本理解 |
| summarize | "总结/摘要"信号 + 长文本 | 文本理解(+记忆) |
| qa | 疑问句 | 文本理解(+kb) |
| plan | 目标 + 多步 | 任务划分(+记忆) |
| logic | 数学/逻辑符号 | 计算/逻辑插件 |
| code.* | 代码/报错/编程请求 | 代码插件 |
| generation.* | 创作/对话/格式信号 | Echo-er（无需插件） |
| retrieval.memory | 历史引用 | 记忆插件 |
| lingua.* | 双语言/切换信号 | 双语插件 |
| kb | 领域实体 | 领域插件 |

**能力映射表 = 仲裁规则的结构化来源**（自动导出，无需手写）：
```
comprehension.* → text_understanding
reasoning.plan  → task_division, memory
code.*          → code 插件
retrieval.memory→ memory
lingua.*        → bilingual
* 且 领域实体命中 → domain_kb（领域插件）
仲裁器粗路由 = 查能力映射表 + 领域命中补充
```

**多意图**：一条输入可有多个意图（"翻译并总结"）→ DispatchRequest 用 `task_types: []` 有序列表，首位为主意图；仲裁按列表逐个查表，插件唤醒取去重并集。

**复杂度定义（complexity）**：
```
complexity = f(任务大类, 输入规模, 状态复杂度, 目标约束数)
- 低档：comprehension / generation / system
- 中档：reasoning.logic / retrieval / code.explain
- 高档：reasoning.plan / 长程 / 多约束 / 需记忆
复杂度 → 仲裁阈值 θ（θ 越低开机越多）
```

**词表治理**：
- 核心词表只增不改（骨架依赖）；领域扩展只增不改（随插件 task_types 并入）。
- 领域子类型带命名空间（如 action.obj），避免与核心冲突。
- 匹配优先级：细类 > 粗类；领域类型 > 通用类型。
- 兜底：unknown → 默认降级（只走主模型 + Echo-er）。

**规则形式化**（纯规则匹配，无模型推理，成本≈0）：
```
RULE := {id, priority, when: COND, then: ACTION}
COND := eq/in/has/regex/flag 谓词 + AND/OR/NOT
ACTION := wake(plugin) | veto(plugin) | request_wake(plugin)
```

**仲裁器规则库（集中，粗）**：`插件 ← task_type 大类条件`
```
state_engine       ← task_type ∈ {action, reason}   # 文本游戏实例
text_understanding ← task_type ∈ {comprehension, reasoning, retrieval, action}
memory             ← task_type ∈ {reasoning, retrieval} ∨ 上下文引用
task_division      ← task_type == plan
bilingual          ← task_type == lingua ∨ 语言切换请求
domain_kb          ← task_type == kb ∨ 领域实体命中
```

**插件判断器规则库（每插件私有，细，双层校验）**：
```
# 通用例：text_understanding 插件的 judge
confirm      ← task_type ∈ {comprehension, reasoning} ∨ 含意图动词
request_wake ← 用户输入含强意图词 ∧ task_type ∉ {comprehension}   # 防漏派
veto         ← task_type == system ∧ 无需文本解析                   # 防误唤醒
# 文本游戏基准实例化时，动词表替换为 {open, take, use, examine, ...}
```

**判定决策表（双层校验完整逻辑）**：
```
对每个插件 p:
 1. 仲裁器粗判定 rel(p, task_type) > θ_arbiter → 候选，发 WakeNotice
 2. 收到 WakeNotice：命中 veto → 拒绝开机（防误唤醒）；否则确认开机
 3. 未被点名的插件探针持续监听：命中 request_wake → 发 WakeRequest（防漏派）
 4. 开机集合 = {确认开机} ∪ {自请求获准}，受背包预算约束
```

**评测**：唤醒精确率（防误唤醒）/ 召回率（防漏派），对照引擎 ground truth 的"最优插件集合"；误唤醒代价=浪费内存/延迟，漏派代价=任务失败或降级。

**规则来源（三层）**：手工（领域知识）+ 引擎自动导出（动作合法表 → 动作→能力映射）+ 运行时阈值随成本卡 g[τ] 微调（任务越复杂 θ 越低、开机越多）。

---

## 6. 训练方案

**数据来源（引擎自动产出，几乎免人工）**：
```
样本 = (x_t, s_t, a_t, s_{t+1}, d*_t, rule*_t, slot_ops*_t)
  x_t 输入/观察文本     s_t / s_{t+1} 引擎真值状态（WS 监督）
  a_t 动作              d*_t 最优调度（引擎 rollout 标注）
  rule*_t 真值规则（动作合法表）       slot_ops*_t 槽更新（(s_t,s_{t+1}) 差异自动提取）
```

**分阶段训练（渐进式）**：
- **阶段 0 数据引擎**：迷你引擎就绪，产出上述监督。
- **阶段 1 单机制预训练**：
  - WS：状态世界模型（mask 状态预测 / 观察+动作→状态迁移），自监督可学。
  - CRM：规则索引学习（`L_match = CE(α, rule*)` + 稀疏正则），软匹配收敛到确定性。
  - 调度头：给定(输入,状态)→最优调度 d*（多意图多标签 CE），不依赖插件真实运行。
- **阶段 2 身份分训**：控制器把 WS+CRM+SVSL+调度头连成完整前向训练；插件、Echo-er 各自任务独立训练。
- **阶段 3 联合微调**：完整回合 rollout，统一损失端到端对齐。
- **阶段 4 课程学习**：难度渐进（单步动作→多步记忆→解谜长程）；先状态一致性、再调度正确性、最后长程推理。
- **阶段 5 强化/自学习**：调度 RL（先 SFT 后 PPO）+ 运行时 gains_overlay 在线更新。

**统一损失目标**：
```
L_total = λ1·L_dispatch(调度 vs d*) + λ2·L_state(WS 读写 vs s*)
        + λ3·L_verify(更新必须通过验证) + λ4·L_crm(规则索引 vs rule*)
        + λ5·L_plugin(插件任务) + λ6·L_echo(生成) + λ7·L_sparsity(稀疏正则)
        + λ8·L_bilingual(跨语言一致：同一状态中英输出一致)
```
λ 随阶段调整：分训时各损失独立，联合时加权。

**跨文件训练（插件是独立文件，不做跨文件反传）**：
- **交替训练（coordinate descent）为主**：固定插件训控制器 ↔ 固定控制器训插件，迭代交替。
- 控制器内部（WS/CRM/SVSL/调度头）本身是一个模型 → 端到端反传。
- 联合微调只在控制器内部做完整端到端；跨身份用交替 + 对接目标。

**离散/不可微处理**：
| 部分 | 训练时 | 推理时 |
|---|---|---|
| 自由槽寻址 | Gumbel straight-through / 软写 | 软写/硬索引 |
| CRM 匹配 | 软 attention（可微） | 编译 DFA 硬匹配 |
| SVSL 循环 | 展开 K 次 + stop-gradient（一个 pass 学验证、一个 pass 学修正） | DFA 确定性检查 |
| 调度多意图 | 多标签 softmax | argmax |

**消融设计（证明每个机制必要）**：
| 消融 | 移除 | 预期影响 |
|---|---|---|
| -WS | 状态读写（用普通 KV） | 状态一致性崩 |
| -CRM | 规则记忆（无 DFA） | 长程/规则任务崩 |
| -SVSL | 验证循环 | 状态污染累积 |
| -调度头 | 随机/规则调度 | 调度正确率降 |
| -三身份 | 单一大模型 | 内存/可扩展崩 |
| -LBNT | 全精度 | 内存超 50MB |

---

## 7. 数据与评测（首个基准：文本游戏）

- **英文**：TextWorld / Jericho / IF Archive，自动产出 `(obs, action, next_obs, state, next_state)` 监督三元组。
- **中文（缺口）**：自建双语迷你引擎（中英动作模板共享状态机）或翻译对齐。
- **CRM 数据**：引擎自动导出"规则级标注"（哪些状态下哪些动作合法）。
- **评测指标**：
  - 状态一致性（20+ 回合 vs 引擎真值）
  - **调度正确率**（控制器选中的插件是否是该任务最优）
  - **组合单调性**（N+1 个插件的系统是否 ≥ N 个）
  - 指令完成率、目标达成率；叙述连贯性；中英双语能力
- **baseline**：同尺寸 vanilla Transformer 微调；Llama-3.2-1B + prompt + JSON 状态；单一大模型（无插件）；单模型含 Echo-er 功能（无三身份解耦）。

---

## 8. 开放决策（做成消融）

1. 控制器规模在 30~50MB 内的甜点；调度策略是"生成调度计划"还是"分类选插件"。
2. 插件接口协议字段粒度与序列化格式。
3. 验证器在校验插件结果 vs 事后抽查之间的配置（默认先验证再生成）。
4. **LBNT**：三值 vs INT4；各身份精度是否一致。
5. 组合单调性的实现机制（模块门控 / 组合归一化 / 插件专属头）。
6. 插件数量上限与"能力阶梯"的粒度。
7. **task_type 词表粒度**：两级是否够用、细类数目。
8. **规则库来源比例**：手工 / 引擎自动导出 / 运行时调优的配比与流程。
9. **仲裁阈值 θ_arbiter 与成本卡 g[τ] 的联动方式**（决定"任务越复杂开机越多"的量化）。
10. **插件市场形态**：集中市场索引 vs 去中心化分发；依赖解析与信任模型（§3.7）。

---

## 9. 研究贡献点（相对已有工作的定位）

| 相关工作 | 区别 |
|---|---|
| 单一 LLM agent（大模型全包） | 决策/知道/说话耦合；本架构三身份解耦且各身份小 |
| MoE | 单 checkpoint 内 token 级路由；本架构是多文件部署级组合，模块间按任务调度 |
| Neural Module Networks | 按输入动态组合（VQA）；本架构是按部署组合、可交付多文件、面向交互式状态建模 |
| Adapter / LoRA 叠加 | 本架构把组合做成"可交付文件 + 能力阶梯 + 单调性目标"的一等公民系统 |
| 黑板架构（Hearsay-II 等） | 本架构把黑板与**电源管理**结合：插件默认关机、仲裁放行才开机，为紧内存预算做硬保证 |
| 通用大模型 + 微调/RAG/Adapter 换领域 | 换领域要重训或挂外部知识；本架构**领域适配 = 下载插件文件即注册即用**，核心骨架不动 |

**贡献表述**：以"唯一主控制器（只调配，内置状态引擎+验证器）+ 可下载能力插件 + 独立 Echo-er（负责整个分析生成输出）"的**通用三身份模型系统**，用 LBNT 低比特原生保证各文件内存（主 30~50MB / 插件与 Echo-er ≤80MB），以可编译规则记忆 CRM 保证状态一致性的结构性可验证，以自验证状态循环 SVSL 保证先验证再生成，以黑板池式 + 电源管理 + 双层仲裁保证紧内存下的可扩展组合；**领域适配=下载插件即注册即用**，核心骨架与领域无关。以文本游戏为首个验证基准，用状态一致性 / 调度正确率 / 组合单调性 / 目标达成率自证。

---

## 10. 风险清单

1. **小控制器调度能力不足**：30~50MB 可能难以胜任复杂调度——最大技术风险。缓解：插件做重活、控制器只做轻调度；调度数据由引擎 rollouts 高质量标注；必要时取 50MB 上限。
2. **组合单调性难保证**：N+1 模块互相干扰导致退化。缓解：接口协议 + 门控/归一化 + 单调性评测门槛。
3. **LBNT 训练稳定性**：三值原生训练收敛慢/不稳。缓解：课程训练、先在迷你引擎验证。
4. **CRM 离散结构难学**：缓解：课程式 + 引擎规则级标注 + 编译正确性评测。
5. **中文语料稀缺**：需自建双语迷你引擎/翻译对齐。
6. **三身份联合微调的一致性**：需消融验证分训+联合 vs 端到端。

---

## 11. 里程碑建议

1. **迷你引擎 + 接口协议原型**：自写双语迷你引擎（首个验证基准：文本游戏），先定"控制器→插件→验证器→Echo-er"的接口协议与最小链路。
2. **单身份验证**：分别验证控制器调度、状态引擎（WS+CRM）、Echo-er 生成能否独立学出（不追求 50MB）。
3. **三身份集成**：完整链路跑通，验证"加插件变强"（组合单调性）。
4. **内存达标**：LBNT 低比特原生 + 精简运行时，锁定 主 30~50MB / 插件与 Echo-er ≤80MB。
5. **研究收尾**：baseline 对照 + 消融 + 论文写作；追加第二个领域插件，验证"下载即用"的通用性。

---

## 12. 储备机制（后续）

- **领域插件市场**：按需下载任意领域插件 → 注册 → 即用，骨架不变；可做"第二个领域"验证通用性。
- **多 Echo-er 变体**：按风格/语言出多个生成器（叙事版、对话版、双语版），由控制器调度。
- **插件热更新**：线上替换单个插件文件即升级能力，无需重训全系统。
